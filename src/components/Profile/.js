{/* <AiFillInfoCircle
              color="#fff"
              size={"2rem"}
              onClick={() => setActive(true)}
              className={`cursor-pointer ${active ? "active" : ""}`}
            />
          </div>
          <div>
            <AiFillSetting
              color="#fff"
              size={"2rem"}
              onClick={() => setActive(true)}
              className={`cursor-pointer ${active ? "active" : ""}`}
            />
          </div>
          <div>
            <SiGooglenews
              color="#fff"
              size={"2rem"}
              onClick={() => setActive(true)}
              className={`cursor-pointer ${active ? "active" : ""}`}
            /> */}